% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{write_vcf_impute}
\alias{write_vcf_impute}
\title{Write imputed per-marker genotypes as a VCF (LB-Impute-style deliverable)}
\usage{
write_vcf_impute(states, path, markers = NULL, ref = "N", alt = "N")
}
\arguments{
\item{states}{A per-marker state table with columns \verb{name, chr, pos, state}
(0/1/2), e.g. from \code{call_states(..., caller = "lbimpute")}.}

\item{path}{Output \code{.vcf} path.}

\item{markers}{Optional data.frame keyed by \verb{chr, pos} carrying \code{ref}, \code{alt}
(and optional \code{id}) alleles to emit; when \code{NULL}, REF/ALT default to \code{N}.}

\item{ref, alt}{Fallback single-character REF/ALT alleles when \code{markers} is
\code{NULL} (default \code{"N"}).}
}
\value{
\code{path}, invisibly.
}
\description{
Optional, decoupled from the engine: turns a per-marker state table (from
\code{\link[=call_states]{call_states()}}, typically \code{caller = "lbimpute"}) into an imputed biallelic
VCF -- LB-Impute's native output form (missing filled, false homozygotes
corrected), as opposed to nilHMM's default segment schema. State -> genotype is
\code{0 -> 0/0} (REF-hom), \code{1 -> 0/1} (het), \code{2 -> 1/1} (ALT-hom); any marker x
sample absent from \code{states} is written \code{./.}. This never re-reads the input
VCF; supply per-marker \code{REF}/\code{ALT} alleles via \code{markers} if you want the real
alleles rather than the \code{N} placeholder.
}
\examples{
st <- data.frame(name = c("NIL1", "NIL1", "NIL2", "NIL2"),
                 chr = 1L, pos = c(1e5, 2e5, 1e5, 2e5),
                 state = c(0L, 2L, 1L, 0L))
write_vcf_impute(st, tempfile(fileext = ".vcf"))
}
