% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_independent.R
\name{select_independent}
\alias{select_independent}
\title{Select a maximal independent set of markers (LD thinning)}
\usage{
select_independent(
  x,
  threshold,
  n_runs = 1L,
  seed = 1L,
  method = c("r2", "mi", "vi"),
  max_markers = 7000L,
  ...
)
}
\arguments{
\item{x}{Genotype matrix (markers x samples) or a precomputed square
relatedness matrix (see Details).}

\item{threshold}{Relatedness cutoff defining an edge (sense per Details).}

\item{n_runs}{Total runs: \code{1} = greedy only; \verb{> 1} adds \code{n_runs - 1} stochastic
runs and records the distinct sets found.}

\item{seed}{Seed for the stochastic runs.}

\item{method}{Measure for the genotype-matrix path and for the edge sense when
\code{x} has no \code{attr(, "kind")}: one of \code{"r2"} (default), \code{"mi"}, \code{"vi"}.}

\item{max_markers}{Safety guard on the marker count (default \code{7000L}, the
validated scale). Selection builds/consumes an O(n^2) markers x markers
matrix, so more than \code{max_markers} markers errors cleanly before any large
allocation (never a \code{malloc} failure); large-but-allowed sizes warn. Raise it
(up to \code{getOption("nilHMM.marker_hard_cap")}, default \code{30000}) only with the
memory to spare -- the intended workflow prunes per chromosome.}

\item{...}{Passed to \code{\link[=pairwise_distance]{pairwise_distance()}} on the genotype path (e.g. \code{base}).}
}
\value{
Character vector of the selected marker names (the largest independent
set), or 1-based integer indices when \code{x} has no names. Attributes:
\code{attr(, "sets")} (list of all distinct sets, as names/indices, greedy first),
\code{attr(, "size_dist")} (named integer vector, set size -> count),
\code{attr(, "kind")} the sense used.
}
\description{
Choose a large set of markers in which no two are more related than
\code{threshold} -- a maximal independent set on the relatedness graph, via a
faithful port of FastIndep (deterministic greedy heuristic + optional
stochastic runs). Intended for pruning a densified genotype matrix per
chromosome before joint-linkage mapping.
}
\details{
\code{x} is EITHER a genotype matrix (rows = markers, cols = samples) OR a
precomputed square relatedness matrix. It is treated as precomputed when it
carries an \code{attr(, "kind")} (as returned by \code{\link[=pairwise_distance]{pairwise_distance()}}) or is a
square symmetric matrix; otherwise it is treated as genotypes and
\code{pairwise_distance(x, method)} is computed. To force one interpretation, pass
the matrix you want.

\strong{Edge / threshold sense} (a set is independent iff no pair is an edge):
\describe{
\item{\code{r2} (similarity)}{edge if \code{r2 >= threshold}. \code{t ~ 0.2} = strict
GWAS-QC pruning; \strong{\code{t ~ 0.5} is recommended for JLM} (keeps resolution,
roughly VIF 2 / PLINK \code{--indep}).}
\item{\code{mi} (similarity)}{edge if \code{MI >= threshold} (nats or bits). For
Gaussian-equivalent reasoning in r2 units, \code{t_MI = -0.5 * ln(1 - r2)}
nats, so \verb{r2 = 0.5 <=> ~0.347 nats} -- given as a note; the code does no
conversion.}
\item{\code{vi} (distance, a metric)}{edge if \code{VI <= threshold} (small VI =
related).}
}
The sense is taken from \code{attr(x, "kind")} when present, else from \code{method}
(\code{vi} is a distance; \code{r2}/\code{mi} are similarities).
}
\section{RNG fidelity}{

The greedy set (\code{n_runs = 1}, or the first reported set) is deterministic and
bit-identical to the FastIndep CLI. The stochastic runs (\code{n_runs > 1}) are
algorithmically faithful and reproducible from \code{seed} (self-contained PRNG),
but individual random sets are not bit-identical to the CLI's Mersenne-Twister
stream.
}

\examples{
set.seed(1)
geno <- matrix(sample(0:2, 8 * 30, replace = TRUE), nrow = 8,
               dimnames = list(paste0("m", 1:8), NULL))
select_independent(geno, threshold = 0.5, method = "r2")
}
\seealso{
\code{\link[=pairwise_distance]{pairwise_distance()}}, \code{\link[=fast_indep_cpp]{fast_indep_cpp()}}
}
