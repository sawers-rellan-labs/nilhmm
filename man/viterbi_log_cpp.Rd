% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{viterbi_log_cpp}
\alias{viterbi_log_cpp}
\title{Generic log-space Viterbi decode (time-homogeneous transitions)}
\usage{
viterbi_log_cpp(log_init, log_trans, log_emit, tie_break = 0L)
}
\arguments{
\item{log_init}{Length-K vector of log initial-state probabilities.}

\item{log_trans}{K x K matrix of log transition probabilities
(row = from-state, column = to-state).}

\item{log_emit}{T x K matrix of log emission probabilities per marker/state.}

\item{tie_break}{Backpointer tie policy for exactly equal predecessor scores:
\code{0} (default) keeps the FIRST (lowest-index) predecessor; \code{1} ("incumbent")
keeps the LAST (highest-index) predecessor. With categorical/GT emissions,
emission-degenerate positions (missing genotype -> all states equal; het ->
REF and donor equal) make the segment boundary a genuine tie; because the
introgression states outrank REF, \code{tie_break = 1} keeps the introgression and
delays the switch back to REF, matching the intent of Holland's \code{hmmlearn}
nNIL caller. Count (BetaBinomial) emissions essentially never tie, so this is
a no-op for them. Only the transition backpointer honours this; the terminal
argmax keeps the first max (as \code{numpy.argmax}).}
}
\value{
Integer length-T most-likely state path, 0-indexed.
}
\description{
Generic log-space Viterbi decode (time-homogeneous transitions)
}
\keyword{internal}
