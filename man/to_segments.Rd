% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine.R
\name{to_segments}
\alias{to_segments}
\title{Collapse per-unit ancestry states into genomic segments}
\usage{
to_segments(states)
}
\arguments{
\item{states}{A per-unit state table from \code{\link[=call_states]{call_states()}}: \verb{name, chr, pos, state} (+ optional \verb{source, donor}; + optional \verb{start_bp, end_bp} for
interval/bin units).}
}
\value{
data.frame in the common schema
(\verb{source, donor, name, chr, start_bp, end_bp, state}).
}
\description{
The coordinate-reinstatement step: run-length-collapses the equal-state runs
within each \verb{(name, chr)} from \code{\link[=call_states]{call_states()}} into segments. \strong{Point} units
(markers) use \code{pos} for both boundaries; \strong{interval} units (bins) carry their
own per-unit \code{start_bp}/\code{end_bp} (as the \code{binhmm} caller emits). This is the
only place genomic coordinates re-enter — the decode itself is coordinate-free.
}
\examples{
st <- data.frame(name = "NIL1", chr = 1L, pos = (1:6) * 1e5L,
                 state = c(0L, 0L, 2L, 2L, 0L, 0L))
to_segments(st)
}
