% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{lb_viterbi_sweep_cpp}
\alias{lb_viterbi_sweep_cpp}
\title{Batched LB-Impute Viterbi over a recombdist grid (shared emission)}
\usage{
lb_viterbi_sweep_cpp(log_init, log_emit, tpos, recombdists, drp)
}
\arguments{
\item{log_init}{Length-3 log initial-state probabilities (REF/HET/ALT).}

\item{log_emit}{T x 3 log emissions (from \code{\link[=lb_emission_loglik_cpp]{lb_emission_loglik_cpp()}}).}

\item{tpos}{Numeric length-T transition coordinate, non-decreasing (bp or cM).}

\item{recombdists}{Numeric grid of recombdist values (same units as \code{tpos}).}

\item{drp}{If \code{TRUE}, a homozygous->homozygous switch costs a single
recombination rather than a double event.}
}
\value{
A T x length(recombdists) integer matrix of state paths (0/1/2);
column k is the decode at \code{recombdists[k]}.
}
\description{
For calibration: \code{recombdist} affects only the transition, never the emission,
so the (memoized) emission matrix is computed once per run and only the Viterbi
transition is re-run for each grid value -- inside C++, avoiding per-value
R<->C++ marshaling. Column \code{k} is \strong{bit-identical} to
\code{\link[=lb_viterbi_cpp]{lb_viterbi_cpp()}} called with \code{recombdist = recombdists[k]}, so a sweep value
equals a cold \code{call_ancestry(caller = "lbimpute", recombdist = v)}.
}
\keyword{internal}
