% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_vcf_gt}
\alias{read_vcf_gt}
\title{Read hard genotype calls (VCF GT) into the engine's observation table}
\usage{
read_vcf_gt(path, samples = NULL)
}
\arguments{
\item{path}{A \code{.vcf} or \code{.vcf.gz} file. \code{GT} must be the first FORMAT field
(the VCF spec requirement when GT is present).}

\item{samples}{Optional character vector to restrict to a subset of samples.}
}
\value{
A long observation table: \verb{name, chr, pos, g}.
}
\description{
Extracts the per-sample \code{GT} field of a \strong{biallelic diploid} VCF into a
\verb{(name, chr, pos, g)} table for the categorical \code{gt} emission -- Holland's
nNIL genotype path, for the saturated-depth regime (e.g. MolBreeding target sequencing at
~20x or more) where the caller's \emph{called} genotype is trustworthy. \code{g} is the
alt-allele dosage: \code{0} REF-hom, \code{1} het, \code{2} ALT-hom, \code{3} missing. Unlike
\code{\link[=read_counts]{read_counts()}} (which feeds the count/BetaBinomial emission from \code{AD} read
depths), this reads the called genotype directly, so it never touches \code{AD}.
}
\details{
Run with \code{call_ancestry(read_vcf_gt(path), caller = "nnil", design = ...)}
(\code{nnil}/\code{catiger} are the categorical gt callers; a \code{g}-only input feeds their
gt emission directly).
}
\examples{
# A minimal biallelic-diploid VCF with a GT field.
f <- tempfile(fileext = ".vcf")
writeLines(c(
  "##fileformat=VCFv4.2",
  "#CHROM\tPOS\tID\tREF\tALT\tQUAL\tFILTER\tINFO\tFORMAT\tNIL1",
  "1\t100000\t.\tA\tT\t.\t.\t.\tGT\t0/0",
  "1\t200000\t.\tA\tT\t.\t.\t.\tGT\t0/1",
  "1\t300000\t.\tA\tT\t.\t.\t.\tGT\t1/1"), f)
read_vcf_gt(f)   # g in {0 REF-hom, 1 het, 2 ALT-hom, 3 missing}
}
