% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refine_ancestry.R
\name{refine_ancestry}
\alias{refine_ancestry}
\title{Refine per-individual ancestry calls over a pedigree}
\usage{
refine_ancestry(
  mosaic,
  pedigree,
  design = "BC2S3",
  emission = c("gt", "count"),
  err = NULL,
  gert = 0.1,
  conc = 20,
  rrate = 0.01,
  maxiter = 30L,
  tol = 1e-04,
  lambda = 0.5,
  ped_format = c("fam", "fsfhap")
)
}
\arguments{
\item{mosaic}{Per-marker table keyed by \verb{name, chr, pos} (+ optional \verb{source, donor, cm}). For \code{emission = "gt"} needs \code{state} in \verb{\{0,1,2,3=missing\}} (from
\code{\link[=call_states]{call_states()}}); for \code{emission = "count"} needs \verb{n_ref, n_alt} read depths.}

\item{pedigree}{A pedigree: a path (read via \code{\link[=read_pedigree]{read_pedigree()}}) or a
\code{\link[=read_pedigree]{read_pedigree()}}-shaped data.frame (\verb{taxon, family, parent1, parent2}).
\code{taxon} joins to \code{mosaic$name}; a \code{taxon} used as a parent but absent from
\code{mosaic} is a latent ancestor.}

\item{design}{Breeding design \code{"BC{n}S{m}"}: the founder prior \code{pi_0} is derived
via \code{\link[=breeding_prior]{breeding_prior()}}; per-node \code{meioses} come from the BC count and pedigree
depth.}

\item{emission}{\code{"gt"} (depth-blind, over hard states) or \code{"count"}
(depth-aware BetaBinomial over \code{n_ref}/\code{n_alt}).}

\item{err}{Genotyping/read error: \code{\link[=emission_gt]{emission_gt()}} \code{germ} when \code{emission="gt"}
(default 0.05), per-read error when \code{emission="count"} (default 0.01).}

\item{gert}{\code{\link[=emission_gt]{emission_gt()}} genotyping-error-rate-in-transmission (\code{"gt"} only).}

\item{conc}{\code{\link[=emission_count]{emission_count()}} BetaBinomial concentration (\code{"count"} only).}

\item{rrate}{Per-bp recombination fraction applied to \code{pos} gaps when \code{mosaic}
has no \code{cm} column (else Haldane on \code{cm}).}

\item{maxiter, tol, lambda}{BP sweeps, convergence tolerance, damping.}

\item{ped_format}{Passed to \code{\link[=read_pedigree]{read_pedigree()}} when \code{pedigree} is a path.}
}
\value{
\code{mosaic} with the refined per-marker calls in its \code{state} column ---
replaced when a \code{state} column is present (\code{emission = "gt"}), or appended when
absent (\code{emission = "count"}) --- preserving the input columns and row order
(genotyped leaves only). Feed to \code{\link[=to_segments]{to_segments()}}.
}
\description{
Thin wrapper on the shared pedigree belief-propagation kernel
(\code{\link[=call_states]{call_states()}} \code{caller = "pedigree"}) for the \strong{hard-call refinement} use:
couple a family's relatives to correct a per-marker mosaic produced by another
caller. Same engine as \code{caller = "pedigree"}; the difference is ergonomic ---
\code{refine_ancestry()} takes and returns a mosaic in place (so a
\code{call_states()} output goes in and a same-shape corrected mosaic comes back),
whereas the caller takes the raw observation table. See design/PEDIGREE_HMM.md.
}
\details{
Two emission modes (the swappable observation channel): \code{"gt"} (default;
depth-blind, \code{\link[=emission_gt]{emission_gt()}} over the hard \code{state} calls) or \code{"count"}
(depth-aware, \code{\link[=emission_count]{emission_count()}} BetaBinomial over read depths). The count mode
is identical to \code{call_ancestry(caller = "pedigree")} on the same counts; prefer
the caller for de novo calling and reserve \code{refine_ancestry()} for correcting an
existing hard-call mosaic.
}
\examples{
\dontrun{
sim   <- simulate_family("BC2S3", families = 10, sibs = 10, seed = 1)
obs   <- simulate_counts(sim$truth, depth = 0.5, seed = 1)
calls <- call_states(obs, caller = "rtiger", design = "BC2S3")
ref   <- refine_ancestry(calls, sim$pedigree, design = "BC2S3")
}
}
\seealso{
\code{\link[=call_states]{call_states()}} (\code{caller = "pedigree"}), \code{\link[=simulate_family]{simulate_family()}},
\code{\link[=pedigree_bp_cpp]{pedigree_bp_cpp()}}, \code{\link[=to_segments]{to_segments()}}.
}
