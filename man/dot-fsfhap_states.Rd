% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsfhap.R
\name{.fsfhap_states}
\alias{.fsfhap_states}
\title{FSFHap caller driver: per-family x chromosome parent-calling + imputation}
\usage{
.fsfhap_states(
  data,
  phet,
  source = "nilHMM",
  donor = NA_character_,
  has_donor = FALSE,
  route = "bc",
  min_gametes = 200L,
  max_missing = 1,
  max_iter = 50L,
  threads = 1L
)
}
\arguments{
\item{data}{Long table with \verb{name, chr, pos, g} (\code{g} in {0,1,2,3}) and a
\code{family} grouping column; optional \code{donor}.}

\item{phet}{Design-derived expected heterozygosity (\code{\link[=.fsfhap_phet]{.fsfhap_phet()}}).}

\item{source, donor}{Output \code{source}/\code{donor} labels (donor per-\code{name} if
\code{has_donor}).}

\item{has_donor}{Whether \code{data} carries a per-row \code{donor} column.}

\item{route}{\code{"bc"} (backcross, BC1) or \code{"biparental"} (BiparentalHaplotypeFinder,
non-BC1) — the design-selected parent-calling route.}

\item{min_gametes, max_missing, max_iter}{Stage-1/3 knobs (TASSEL 200 / 1.0 / 50).}

\item{threads}{Fan-out width for the family x chromosome units. Each unit is
fully independent (chromosome-separate processing, family-pooled), so it maps
cleanly onto \code{\link[parallel:mclapply]{parallel::mclapply()}} (unix only; serial \code{\link[=lapply]{lapply()}} elsewhere).
This is FSFHap's coarse-grained parallelism — the single-threaded per-unit
stages 1+3 are untouched, so results are identical to \code{threads = 1L}.}
}
\value{
\code{data.frame(source, donor, name, chr, pos, state)}, \code{state} in {0,1,2};
uncalled markers (dropped by the filters, or still missing after gap-fill) are
simply absent. Feed to \code{\link[=to_segments]{to_segments()}}.
}
\description{
The \code{caller = "fsfhap"} path for \code{\link[=call_states]{call_states()}}. Unlike the per-sample callers,
FSFHap \strong{pools each family}: for every \verb{family x chr} it builds the taxa x sites
genotype matrix, runs BC parent-calling (\code{\link[=.fsfhap_call_parents_bc]{.fsfhap_call_parents_bc()}}, stage 1)
then the 5-state EM imputation + gap-fill (\code{\link[=.fsfhap_impute]{.fsfhap_impute()}}, stage 3), and
emits per-marker states in the common REF/HET/ALT frame (major allele = the
recurrent parent in a backcross → \code{0} REF, \code{1} HET, \code{2} ALT/donor).
}
\keyword{internal}
