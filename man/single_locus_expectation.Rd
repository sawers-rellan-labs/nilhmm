% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breeding_design.R
\name{single_locus_expectation}
\alias{single_locus_expectation}
\title{Single-locus (Mendelian) genotype expectation for a breeding design}
\usage{
single_locus_expectation(design, f1 = c(0, 1, 0))
}
\arguments{
\item{design}{Design key of the form \code{"BC<n>S<m>"} (e.g. \code{"BC2S2"}, \code{"BC1S4"}).}

\item{f1}{Starting genotype-frequency vector in \verb{(AA, Aa, aa)} order (recurrent
hom, het, donor hom). Defaults to \code{c(0, 1, 0)}, the fully-heterozygous F1 of
inbred parents.}
}
\value{
Named numeric vector \code{c(REF, HET, ALT)} summing to 1, mapping the
\verb{(AA, Aa, aa)} genotypes to the REF/HET/ALT ancestry states.
}
\description{
Propagates the single-locus genotype-frequency vector through the backcross and
selfing transition matrices for the design named by \code{design} (\code{"BC<n>S<m>"}):
\code{n} backcrosses to the recurrent parent, then \code{m} generations of selfing. The
genotype basis is \verb{(AA, Aa, aa)} with \code{AA} the recurrent (backcross-target)
homozygote and \code{aa} the donor homozygote, so the recurrent allele is \code{A}
(frequency \code{p_A}) and the donor allele is \code{a} (frequency \code{p_a = 0.5^(n + 1)},
fixed by the backcrosses and invariant across selfing). The backcross matrix \code{B}
mates each generation to \code{AA} (\code{aa -> Aa}, \verb{Aa -> half AA / half Aa}) and the
selfing matrix \code{S} splits a quarter of the remaining \code{Aa} into equal parts
\code{AA}/\code{aa} each generation.
}
\details{
The starting composition \code{f1} is a free argument, so the expectation is exact
for a non-inbred F1, not only the fully-heterozygous \code{c(0, 1, 0)} case that
inbred parents give. At the inbred default, BC2S2 lands at REF 0.844 /
HET 0.0625 / ALT 0.0938.
}
\examples{
single_locus_expectation("BC2S2")                       # inbred parents
single_locus_expectation("BC2S2", f1 = c(0.5, 0.5, 0))  # non-inbred F1
}
\seealso{
\code{\link[=parse_design]{parse_design()}}, \code{\link[=breeding_prior]{breeding_prior()}}
}
