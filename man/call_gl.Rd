% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_gl.R
\name{call_gl}
\alias{call_gl}
\title{Per-site genotype-likelihood (GL) genotype caller with a swappable prior}
\usage{
call_gl(
  n_ref,
  n_alt,
  prior = c("hwe", "flat", "breeding"),
  af = NULL,
  error = 0.01,
  f = NULL,
  return = c("call", "dosage", "post")
)
}
\arguments{
\item{n_ref, n_alt}{Reference / alternate read counts. Integer (or numeric)
matrices \verb{markers x samples}, or plain vectors (treated as one sample =
\verb{markers x 1}). Same shape required.}

\item{prior}{One of \code{"hwe"} (default), \code{"flat"}, \code{"breeding"}.}

\item{af}{For \code{prior = "hwe"}: per-marker ALT allele frequency \code{p}, length
\code{nrow}. \code{NULL} (default) estimates \code{p} from the reads.}

\item{error}{Per-read error rate \code{epsilon} in \verb{(0, 0.5)} (default \code{0.01}).}

\item{f}{For \code{prior = "breeding"}: length-3 genotype frequencies
\code{c(f_REF, f_HET, f_ALT)} (renormalized).}

\item{return}{\code{"call"} (default): integer 0/1/2 hard call, \code{NA} at zero depth.
\code{"dosage"}: the same hard call as numeric (convenience for
\code{\link[=interpolate_genotype]{interpolate_genotype()}}). \code{"post"}: the normalized posterior array
\verb{markers x samples x 3} (\code{NA} rows at zero depth).}
}
\value{
For \code{"call"}/\code{"dosage"}: a matrix \verb{markers x samples} (a plain vector if
the inputs were vectors). For \code{"post"}: a \verb{markers x samples x 3} array.
}
\description{
A closed-form GATK-style diploid genotype call from allelic read counts, decided
independently per (marker, sample) -- no linkage model. ALT = donor (teosinte) =
dosage 2. Given per-read error \code{error} and counts \verb{(n_ref, n_alt)}, the three
genotype log-likelihoods are
\deqn{L(0) = (1-\epsilon)^{n_{ref}}\,\epsilon^{n_{alt}}}
\deqn{L(2) = \epsilon^{n_{ref}}\,(1-\epsilon)^{n_{alt}}}
\deqn{L(1) = 0.5^{\,n_{ref}+n_{alt}}}
and the call is \verb{argmax_g L(g) * pi(g)} over the prior \code{pi}. All arithmetic is in
log space and vectorized over the whole matrix (no per-cell loop). Markers with
zero total depth carry no signal and are returned as \code{NA}.
}
\details{
Priors (\code{prior}):
\describe{
\item{\code{"hwe"}}{Hardy-Weinberg from the per-marker ALT (donor/teosinte) allele
frequency \code{p}: \verb{pi = \{(1-p)^2, 2p(1-p), p^2\}}. This is the \strong{het-excess}
control -- a single ALT read is called HET. \code{af} supplies \code{p} per marker; if
\code{NULL}, \code{p} is estimated per marker from the reads
(\code{rowSums(n_alt) / rowSums(n_ref + n_alt)}).}
\item{\code{"flat"}}{Uniform prior -> pure argmax-GL. Het-blind at depth 1 (a single
ALT read is called hom-ALT), the pessimistic naive caller.}
\item{\code{"breeding"}}{Fixed design frequencies \code{f = c(f_REF, f_HET, f_ALT)}
(e.g. BC1S4 ~ \code{c(0.84, 0.06, 0.10)}); renormalized to sum 1.}
}
}
\examples{
# A single ALT read: het-blind under flat, HET under HWE (het-excess control).
call_gl(0, 1, prior = "flat")               # 2 (hom-ALT)
call_gl(0, 1, prior = "hwe", af = 0.3)       # 1 (HET)
# High depth: the data dominates the prior.
call_gl(0, 10, prior = "hwe", af = 0.05)     # 2
}
\seealso{
\code{\link[=interpolate_genotype]{interpolate_genotype()}}, \code{\link[=call_ancestry]{call_ancestry()}}
}
