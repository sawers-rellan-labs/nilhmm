% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{build_marker_grid}
\alias{build_marker_grid}
\title{Build a physical marker grid from a genetic map}
\usage{
build_marker_grid(map, n_markers = 2000L)
}
\arguments{
\item{map}{A map with columns \code{chr}, \code{cm}, \code{bp} (e.g. from \code{\link[=load_map]{load_map()}}).}

\item{n_markers}{Approximate total marker count across all chromosomes.}
}
\value{
data.frame \code{chr}, \code{pos} (bp), \code{cm}, sorted by \verb{chr, pos}.
}
\description{
Spread \code{n_markers} markers across the chromosomes of \code{map} in proportion to
physical span, interpolating each marker's cM from the map (monotone Hyman
spline, bp -> cM). This is how \code{\link[=simulate_nil]{simulate_nil()}} samples the bundled consensus
map (\code{\link[=load_map]{load_map()}}) at a chosen density.
}
\examples{
g <- build_marker_grid(load_map(), n_markers = 500)
table(g$chr)
}
\seealso{
\code{\link[=load_map]{load_map()}}, \code{\link[=simulate_nil]{simulate_nil()}}
}
