% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{paint_calls}
\alias{paint_calls}
\title{Chromosome-paint called ancestry segments}
\usage{
paint_calls(
  calls,
  track = NULL,
  samples = NULL,
  palette = c(REF = "gold", HET = "springgreen4", ALT = "purple4")
)
}
\arguments{
\item{calls}{Segment calls in the common schema: columns \code{name}, \code{chr},
\code{start_bp}, \code{end_bp}, \code{state} (\code{0}/\code{1}/\code{2}). Extra columns are ignored.}

\item{track}{Optional column name whose levels are stacked as bands within each
\code{name} x \code{chr} cell (e.g. \code{"method"} for a caller comparison, or \code{"donor"}).
\code{NULL} (default) paints one band per sample. A factor \code{track} keeps its level
order (top band = first level).}

\item{samples}{Optional character vector restricting to a subset of \code{name}
values (keeps busy figures legible).}

\item{palette}{Named length-3 fill for \code{c(REF, HET, ALT)}.}
}
\value{
A \pkg{ggplot2} object (requires the suggested \pkg{ggplot2}).
}
\description{
Paint the common-schema segments as coloured rectangles along each chromosome
--- REF / HET / ALT (\code{state} 0/1/2). Each \strong{sample} (\code{name}) is a facet row and
each chromosome a facet column. When \code{track} is supplied, its levels are stacked
as horizontal bands \emph{within} every cell, so several callers (or data sources)
can be overlaid on identical axes to check whether they agree on where the donor
blocks land --- the "sanity paint" comparison. Build the multi-track input by
\code{rbind()}-ing each caller's \code{\link[=call_ancestry]{call_ancestry()}} output with a column naming the
track, then pass that column name as \code{track}.
}
\examples{
calls <- data.frame(
  name = "NIL01", donor = "B", chr = 1L,
  start_bp = c(1e6, 3e6, 5e6), end_bp = c(3e6, 5e6, 9e6),
  state = c(0L, 2L, 0L))
if (requireNamespace("ggplot2", quietly = TRUE)) paint_calls(calls)
}
