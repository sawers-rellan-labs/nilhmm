% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_pedigree}
\alias{read_pedigree}
\title{Read a pedigree (FSFHap TSV or PLINK .fam) into a family/priors table}
\usage{
read_pedigree(path, format = c("fsfhap", "fam"))
}
\arguments{
\item{path}{A pedigree file.}

\item{format}{\code{"fsfhap"} (default) or \code{"fam"}.}
}
\value{
data.frame \verb{taxon, family, parent1, parent2, contribution1, contribution2, F}.
}
\description{
The IO adapter for FSFHap family grouping + design priors (keeps the caller
data-agnostic: the algorithm never reads files). Returns one row per taxon.
}
\details{
\itemize{
\item \code{format = "fsfhap"}: TASSEL's 7-column, header-first FSFHap pedigree
(\verb{family taxon parent1 parent2 contribution1 contribution2 F}).
\item \code{format = "fam"}: PLINK \code{.fam} (\verb{FID IID PID MID sex pheno}) -> \code{family} =
FID, \code{taxon} = IID; contribution/\code{F} are \code{NA} (not in \code{.fam}).
}

Use it to attach the \code{family} key and derive the design for \code{caller = "fsfhap"}:
\verb{ped <- read_pedigree(p); data$family <- ped$family[match(data$name, ped$taxon)]};
then pass \code{design}/\code{phet} (phet = \code{(1 - F)/2}, see \code{\link[=.fsfhap_phet]{.fsfhap_phet()}}).
}
\examples{
p <- tempfile()
writeLines(c("family\ttaxon\tparent1\tparent2\tp1\tp2\tF",
             "SIM\tL1\tA\tC\t0.75\t0.25\t0.9375"), p)
read_pedigree(p)
}
