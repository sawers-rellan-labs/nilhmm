% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rtiger_fit_cpp}
\alias{rtiger_fit_cpp}
\title{RTIGER full EM fit (E-step + M-step + convergence loop, all in C++)}
\usage{
rtiger_fit_cpp(
  ks_list,
  ns_list,
  r,
  nstates,
  eps,
  max_iter,
  threads,
  init_alpha,
  init_beta
)
}
\arguments{
\item{ks_list, ns_list}{Lists of per-chain integer (k, n) vectors.}

\item{r, nstates}{Rigidity and number of states.}

\item{eps, max_iter}{Convergence tolerance and iteration cap.}
}
\value{
list(A, pi, alpha, beta, iterations).
}
\description{
The entire fit (port of the fork's \code{fit}/\code{EM}): per-chain rigidity E-step,
pooled M-steps (transition, start, emission via C++ Brent), iterated until
max(|Δα|,|Δβ|) <= eps or max.iter. Deterministic init (generate_params forms,
randomize off). Returns the fitted parameters and iteration count.
}
\keyword{internal}
