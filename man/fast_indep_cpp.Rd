% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fast_indep_cpp}
\alias{fast_indep_cpp}
\title{Find a large independent set in a thresholded similarity/distance matrix}
\usage{
fast_indep_cpp(sim, threshold, n_runs, seed, distance = FALSE)
}
\arguments{
\item{sim}{Symmetric numeric matrix. A similarity (higher = more related,
\code{distance = false}) or a distance (lower = more related, \code{distance = true}).}

\item{threshold}{Edge cutoff. Similarity: edge if \code{sim[i,j] >= threshold}.
Distance: edge if \code{sim[i,j] <= threshold}.}

\item{n_runs}{Total runs: 1 = greedy only; \verb{> 1} adds \code{n_runs - 1} stochastic
runs and returns the distinct sets found.}

\item{seed}{Seed for the stochastic runs (self-contained splitmix64 PRNG;
independent of R's RNG). The greedy run is deterministic and seed-free.}

\item{distance}{If \code{true}, \code{sim} is a distance and the edge sense is inverted
(edge if \verb{<= threshold}). Default \code{false} (similarity).}
}
\value{
A list: \code{best} (1-based indices of the largest independent set found,
the greedy set when it is largest), \code{sets} (list of distinct sets, greedy
first then by decreasing size), \code{size_dist} (named integer vector, set
size -> number of distinct sets of that size).
}
\description{
Faithful port of FastIndep (Abraham 2013): the deterministic greedy heuristic
plus \code{n_runs - 1} stochastic runs. Two nodes are "related" (an edge) when the
matrix entry crosses \code{threshold} in the sense given by \code{distance}; an
independent set has no edges among its members. The matrix diagonal is ignored.
}
\keyword{internal}
