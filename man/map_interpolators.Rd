% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{map_interpolators}
\alias{map_interpolators}
\alias{bp_to_cm}
\alias{cm_to_bp}
\title{Map interpolators: physical <-> genetic position}
\usage{
bp_to_cm(map = load_map())

cm_to_bp(map = load_map())
}
\arguments{
\item{map}{A consensus map with columns \code{chr}, \code{bp}, \code{cm}. Defaults to
\code{\link[=load_map]{load_map()}} (bundled B73 v5).}
}
\value{
\code{bp_to_cm}: \verb{function(chr, bp) -> cm}; \code{cm_to_bp}: \verb{function(chr, cm) -> bp}.
}
\description{
Build a per-chromosome monotone Hyman spline (a Marey map) through a consensus
map's \verb{(bp, cm)} pairs, clamped to each chromosome's observed range, and return
a vectorized \verb{function(chr, x)}. Duplicate coordinates are collapsed by mean.
The default map is the bundled B73 v5 consensus map; pass any data frame with
\code{chr}, \code{bp}, \code{cm} columns to use your own (e.g. a population's native map).
}
\examples{
to_cm <- bp_to_cm()          # bundled map
to_cm(1L, 1e6)
}
\seealso{
\code{\link[=cm_to_mb]{cm_to_mb()}}, \code{\link[=load_map]{load_map()}}
}
