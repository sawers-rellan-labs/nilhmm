% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsfhap.R
\name{.fsfhap_segregating}
\alias{.fsfhap_segregating}
\title{FSFHap stage 1a: segregating-site test for one family x chromosome}
\usage{
.fsfhap_segregating(G, max_missing = 0.9, ratio)
}
\arguments{
\item{G}{Integer matrix, taxa x sites, values 0/1/2/3 = REF-hom / het /
ALT-hom / missing (engine canonical \code{g}); sites sorted by position.}

\item{max_missing}{Max missing-genotype proportion for a site to be tested
(TASSEL default 0.9; the TeoNAM run passes \code{1.0}).}

\item{ratio}{Expected minor-allele frequency selecting the segregation model:
\code{0.25}/\code{0.75} (backcross) or \code{0.5} (F2). \strong{Design-derived, no default} — it
must be supplied by the caller from the breeding design (the routing
dispatcher, mirroring TASSEL's \code{contribution1} → route → ratio), never
guessed or defaulted here.}
}
\value{
A list: \code{seg} (logical, kept sites), \code{Mj}/\code{Mn} (major/minor allele
counts), \code{p_missing}, and model probabilities \code{pmono}/\code{pquarter}/\code{phalf}.
}
\description{
Thin wrapper over \code{\link[=fsfhap_segregating_sites_cpp]{fsfhap_segregating_sites_cpp()}} — a faithful port of
TASSEL's \code{whichSitesSegregateCorrectly}. Given one family's genotype matrix on
one chromosome, flags the sites whose minor-allele count fits the family's
segregation model (backcross vs F2) better than a monomorphic/error model.
}
\details{
\code{ratio} selects the branch, matching TASSEL: \code{0.25}/\code{0.75} → backcross (keep
iff \code{pquarter > phalf && pquarter > pmono}), anything else → F2 (\code{phalf / (pmono + pquarter) > 2}). Note TASSEL's backcross branch always tests the
\strong{0.25} model regardless of backcross depth, so deeper backcrosses (e.g. a
BC2 donor fraction ~0.125) are tested against 0.25 too; the port reproduces
this deliberately for parity.
}
\keyword{internal}
