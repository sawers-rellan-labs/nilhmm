% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{read_plink}
\alias{read_plink}
\title{Read a PLINK binary genotype (.bed/.bim/.fam) into the engine's table}
\usage{
read_plink(prefix)
}
\arguments{
\item{prefix}{Path prefix (the \code{.bed}, \code{.bim}, \code{.fam} trio); a trailing
\code{.bed} is stripped if given.}
}
\value{
A long observation table: \verb{name, chr, pos, g}.
}
\description{
Adapter for PLINK 1 binary genotypes into the canonical \verb{(name, chr, pos, g)}
table. \code{g} is the \strong{A1-allele dosage} (A1 = \code{.bim} column 5, PLINK's minor
allele by default): \code{2} = A1-hom, \code{1} = het, \code{0} = A2-hom, \code{3} = missing.
SNP-major \code{.bed} only (the PLINK default).
}
\seealso{
\code{\link[=read_pedigree]{read_pedigree()}} (\code{format = "fam"}) for the family grouping.
}
