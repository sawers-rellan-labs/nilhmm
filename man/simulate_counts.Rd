% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_counts}
\alias{simulate_counts}
\title{Degrade simulated truth to observed allelic read counts}
\usage{
simulate_counts(truth, depth = 1, error = 0.01, p_missing = 0, seed = NULL)
}
\arguments{
\item{truth}{A \code{\link[=simulate_nil]{simulate_nil()}} table (needs \verb{name, donor, chr, pos, state}).}

\item{depth}{Mean per-marker read depth (Poisson mean).}

\item{error}{Per-read error rate (homozygote miscall probability).}

\item{p_missing}{Extra fraction of markers set missing (on top of zero-depth).}

\item{seed}{Optional RNG seed.}
}
\value{
data.frame \verb{name, donor, chr, pos, n_ref, n_alt, g} (\code{g} in \verb{\{0,1,2,3\}}).
}
\description{
Turn a \code{\link[=simulate_nil]{simulate_nil()}} truth table into observed data under a generic
sequencing model: per marker draw depth \code{~ Poisson(depth)}, mask a fraction
\code{p_missing} (and zero-depth markers) to missing, and draw ALT reads
\code{~ Binomial(depth, p)} where \code{p} is the state's donor fraction (\code{0} / \code{0.5} / \code{1})
flipped by the per-read \code{error}. Returns the \code{read_counts()}-style columns plus a
hard genotype \code{g} (masked to \code{3} where missing), so the result drives both the
count callers (\code{n_ref}/\code{n_alt}) and the genotype callers (\code{g}). The named
experiment-specific coverage regimes (skim / BRB / target) live in the pipeline,
not here.
}
\examples{
if (requireNamespace("simcross", quietly = TRUE)) {
  truth <- simulate_nil("BC2S2", n = 2, chr = 1:2, n_markers = 100, seed = 1)
  obs <- simulate_counts(truth, depth = 6, seed = 1)
  head(obs)
}
}
\seealso{
\code{\link[=simulate_nil]{simulate_nil()}}, \code{\link[=read_counts]{read_counts()}}.
}
