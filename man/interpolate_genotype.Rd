% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate_genotype.R
\name{interpolate_genotype}
\alias{interpolate_genotype}
\title{Interpolate genotypes onto a target marker grid (Tian 2011 / Chen 2019)}
\usage{
interpolate_genotype(
  geno,
  obs,
  target,
  mode = c("continuous", "step", "round")
)
}
\arguments{
\item{geno}{Numeric matrix, \code{nrow(geno) == nrow(obs)}, column = sample;
COMPLETE (no \code{NA}). Values are the alt-allele dosage in \verb{[0, 2]}.}

\item{obs}{\code{data.frame(chr, cm)} aligned row-for-row to \code{geno}, sorted by
\verb{(chr, cm)} with \code{cm} strictly increasing within each chromosome.}

\item{target}{\code{data.frame(chr, cm)} of the target grid, sorted by \verb{(chr, cm)}.}

\item{mode}{One of \code{"continuous"} (Tian), \code{"step"} (Chen/TeoNAM), \code{"round"}.}
}
\value{
Numeric matrix \code{nrow(target)} x \code{ncol(geno)}; rownames from \code{target}
(if any), colnames from \code{geno}.
}
\description{
Densifies a complete genotype block onto a common target marker grid by
flanking-marker interpolation in genetic distance (cM). For a target between
called flanking markers L and R, the interpolation weight is
\code{w = (cM_x - cM_L) / (cM_R - cM_L)} and the continuous dosage is
\code{v_L + w * (v_R - v_L)} -- the Tian 2011 rule (the weighted average
interpreted as the probability the SNP comes from the non-recurrent parent).
Ends are clamped to the terminal observed value (equivalent to
\code{\link[stats:approx]{stats::approx()}} with \code{rule = 2}).
}
\details{
Genotypes are the alt/teosinte-allele dosage: \code{0} = REF/recurrent hom,
\code{1} = HET, \code{2} = ALT/donor hom (\code{continuous} returns real values in \verb{[0, 2]}).
Three modes share one interpolation core:
\describe{
\item{\code{continuous}}{dosage ramp (Tian 2011); returns fractional dosage.}
\item{\code{step}}{nearest flanking value (\code{w < 0.5 ? v_L : v_R}, hard step at the
cM midpoint, tie \code{w == 0.5} -> \code{v_R}); the faithful Chen/TeoNAM discrete
form -- fabricates no het across a \verb{0<->2} gap.}
\item{\code{round}}{\code{round(continuous)} to 0/1/2; fabricates a het band across a
\verb{0<->2} gap (width proportional to the cM gap) -- provided to demonstrate
that artifact.}
}

The function densifies \strong{one dense block} to a target grid. For block-sparse
per-family data (each family genotyped on its own marker subset), loop over
families -- interpolating each family's dense block onto the shared grid -- and
\code{cbind} the results; that loop lives in the consumer pipeline, not here.

\code{geno} columns are assumed COMPLETE (no \code{NA}) -- true for imputed truth and for
HMM-caller output. There is no \code{NA}-aware flanking search.
}
\examples{
obs <- data.frame(chr = 1L, cm = c(0, 1))
geno <- matrix(c(0, 2), nrow = 2, dimnames = list(NULL, "S1"))
target <- data.frame(chr = 1L, cm = c(0, 0.5, 1))
interpolate_genotype(geno, obs, target, "continuous")  # 0, 1, 2
}
