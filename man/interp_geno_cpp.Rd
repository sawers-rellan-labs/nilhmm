% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{interp_geno_cpp}
\alias{interp_geno_cpp}
\title{Interpolate a complete genotype block onto a target cM grid (single chr)}
\usage{
interp_geno_cpp(obs_cm, G, target_cm, mode)
}
\arguments{
\item{obs_cm}{Numeric cM of the observed markers, ascending, length k.}

\item{G}{Numeric k x n genotype matrix (row = observed marker, col = sample),
COMPLETE (no NA); values are the alt/teosinte-allele dosage in 0 to 2.}

\item{target_cm}{Numeric cM of the target grid, ascending, length M.}

\item{mode}{Interpolation mode: 0 = continuous dosage ramp (Tian 2011),
1 = step / nearest flanking value (\code{w < 0.5 ? vL : vR}, tie \code{w == 0.5} -> vR;
Chen/TeoNAM), 2 = round(continuous) to 0/1/2.}
}
\value{
Numeric M x n matrix of interpolated genotypes.
}
\description{
Linear flanking-marker interpolation in genetic distance for one chromosome.
For a target at cM \code{t} between called flanking markers L (at \code{obs_cm[jj]}) and
R (at \code{obs_cm[jj+1]}), the weight is \code{w = (t - cM_L) / (cM_R - cM_L)} and the
continuous dosage is \code{vL + w*(vR - vL)}. Ends are clamped to the terminal
observed value (\code{stats::approx} rule = 2); tied flanking positions
(\code{denom == 0}) collapse to \code{w = 0}.
}
\keyword{internal}
