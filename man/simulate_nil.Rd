% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_nil}
\alias{simulate_nil}
\title{Simulate the true donor mosaic of a cross via simcross}
\usage{
simulate_nil(
  design = "BC2S2",
  n = 1L,
  map = NULL,
  n_markers = 2000L,
  chr = NULL,
  m = 10L,
  p = 0,
  seed = NULL,
  donor = "B",
  names = NULL,
  pedigree = NULL,
  nil_id = NULL
)
}
\arguments{
\item{design}{Breeding design \code{"BC{n}S{m}"} (e.g. \code{"BC2S2"}, \code{"BC1S4"}). Ignored
when \code{pedigree} is supplied.}

\item{n}{Number of lines to simulate (independent meioses).}

\item{map}{Reference genetic map with \code{chr}, \code{cm}, \code{bp}. \code{NULL} (default) uses
the bundled \code{\link[=load_map]{load_map()}} (\code{maize_map_v5}).}

\item{n_markers}{Approximate marker count to sample from \code{map} (\code{\link[=build_marker_grid]{build_marker_grid()}}).}

\item{chr}{Optional integer vector restricting to a subset of chromosomes.}

\item{m, p}{simcross Stahl interference (\code{m = 10}, \code{p = 0} ~ maize-like).}

\item{seed}{Optional RNG seed for reproducibility.}

\item{donor}{Donor/ALT label for the output \code{donor} column.}

\item{names}{Optional length-\code{n} sample names (default \code{sim0001} ...).}

\item{pedigree, nil_id}{Escape hatch: a ready simcross pedigree
(\verb{id, mom, dad, sex, gen}) and the id to sample; bypasses \code{design}.}
}
\value{
data.frame \verb{source, donor, name, chr, pos, cm, state} (\code{state} 0/1/2),
one row per (line, marker), sorted by \verb{name, chr, pos}.
}
\description{
Design-driven truth generator: build the pedigree for a \code{"BC{n}S{m}"} design
(F1, then \code{n} backcrosses to the recurrent parent, then \code{m} selfings), simulate
it with \pkg{simcross} (real meiosis / crossover interference on a genetic map),
and read off each line's \strong{true} donor dosage at the markers. The result is a
per-marker table with \code{state} in \verb{\{0 REF, 1 HET, 2 ALT\}} (ALT = donor) that feeds
\code{\link[=to_segments]{to_segments()}} (truth segments), \code{\link[=paint_calls]{paint_calls()}} (a truth track), or
\code{\link[=simulate_counts]{simulate_counts()}} (degrade to observed read counts). Recurrent parent = REF = A;
donor = ALT.
}
\details{
Data-agnostic: defaults to the bundled B73 v5 consensus map (\code{\link[=load_map]{load_map()}}),
sampled to \code{n_markers} by \code{\link[=build_marker_grid]{build_marker_grid()}}; pass your own \code{map} to override.
Hardcodes no design parameters (the design token drives the pedigree). For
designs beyond backcross-self (RIL, AIL, MAGIC, ...), pass a ready simcross
\code{pedigree} + \code{nil_id} and \code{design} is used only to label output.
}
\examples{
if (requireNamespace("simcross", quietly = TRUE)) {
  truth <- simulate_nil("BC2S2", n = 2, chr = 1:2, n_markers = 200, seed = 1)
  head(truth)
  to_segments(truth)          # true donor segments
}
}
\seealso{
\code{\link[=load_map]{load_map()}}, \code{\link[=build_marker_grid]{build_marker_grid()}}, \code{\link[=simulate_counts]{simulate_counts()}}, \code{\link[=to_segments]{to_segments()}}.
}
