% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fsfhap.R
\name{.fsfhap_call_parents_bc}
\alias{.fsfhap_call_parents_bc}
\title{FSFHap stage 1b: backcross parent-allele calling for one family x chromosome}
\usage{
.fsfhap_call_parents_bc(
  G,
  pos,
  max_missing = 1,
  min_rsq = 0.8,
  min_gametes = 200L,
  min_r = 0
)
}
\arguments{
\item{G}{Integer matrix, taxa x sites, canonical \code{g} in {0,1,2,3}; one family,
one chromosome, sites sorted by position.}

\item{pos}{Integer marker positions (bp), length = ncol(G).}

\item{max_missing}{Max missing proportion for the segregating-site test
(TASSEL default 0.9; TeoNAM run 1.0).}

\item{min_rsq}{Same-tag R^2 threshold (TASSEL 0.8).}

\item{min_gametes}{Coverage floor: taxa with \verb{> min_gametes} non-missing
gametes (2 x non-missing sites) are kept (TASSEL 200).}

\item{min_r}{LD-filter threshold; \verb{> 0} enables TASSEL's \code{ldfilter}, \strong{not yet
ported} (warns and ignores). The TeoNAM run uses \code{0}.}
}
\value{
List: \code{G} (recoded, kept taxa x kept sites), \code{keep_sites}/\code{keep_taxa}
(indices into the input), \code{pos} (kept-site positions), \code{major_is_ref}
(per kept site), and counts \code{n_seg}/\code{n_sametag}/\code{n_kept_sites}.
}
\description{
Faithful orchestration of TASSEL's \code{callParentAllelesByWindowForBackcrosses}:
keep sites that both segregate as backcross (stage 1a) and survive the same-tag
filter (\code{\link[=fsfhap_same_tag_keep_cpp]{fsfhap_same_tag_keep_cpp()}}); assign A = major / C = minor per kept
site; drop low-coverage taxa (\verb{<= min_gametes} non-missing gametes across kept
sites); recode genotypes into the parent-origin A/C frame
(\code{0} = A-hom / \code{1} = het / \code{2} = C-hom / \code{3} = missing), where A is the site's
major allele. The result is the observation matrix the 5-state smoother
consumes next.
}
\keyword{internal}
