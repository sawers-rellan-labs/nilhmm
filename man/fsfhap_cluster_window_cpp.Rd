% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fsfhap_cluster_window_cpp}
\alias{fsfhap_cluster_window_cpp}
\title{FSFHap stage 2a: cluster a window of parent-called haplotypes}
\usage{
fsfhap_cluster_window_cpp(
  Gw,
  maxdiff = 0L,
  merge = FALSE,
  move_biggest = FALSE,
  max_het = -1L
)
}
\arguments{
\item{Gw}{Integer matrix, taxa x window-sites, canonical \code{g} in \verb{\{0,1,2,3\}}.}

\item{maxdiff}{Distance threshold for \code{merge}/\code{move_biggest} (TASSEL
\code{maxDifferenceScore}, 0 on the BC/finder path).}

\item{merge}{Apply \code{mergeClusters(maxdiff)} after \code{makeClusters}
(clusterWindow does this only when \code{maxdiff > 0}).}

\item{move_biggest}{Apply \code{moveAllHaplotypesToBiggestCluster(maxdiff)}.}

\item{max_het}{If \verb{>= 0}, drop clusters with more than \code{max_het} heterozygous
sites (\code{removeHeterozygousClusters}; the finder passes \code{maxdiff + 5}).}
}
\value{
List: \code{size}, \code{score} (per cluster); \code{majority}, \code{unanimous}
(clusters x sites consensus, \code{3} = N); \code{members} (list of 1-based taxon
indices per cluster). For 0-distance clusters \code{majority == unanimous};
they diverge only after merges.
}
\description{
Faithful port of \code{HaplotypeClusterer.makeClusters} + \code{HaplotypeCluster}
consensus, on the parent-origin frame (\code{g} in \verb{\{0 A-hom, 1 het, 2 C-hom, 3 missing\}}).
Clusters group taxa whose window haplotypes are 0-distance
(identical modulo missing); a haplotype 0-distance to members of several
clusters joins all of them with fractional score \code{1/count}. Clusters are
returned sorted by score (desc) then size (desc) — \code{HaplotypeCluster.compareTo}.
}
\keyword{internal}
