% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fsfhap_segregating_sites_cpp}
\alias{fsfhap_segregating_sites_cpp}
\title{FSFHap segregating-site test (whichSitesSegregateCorrectly), faithful port}
\usage{
fsfhap_segregating_sites_cpp(G, max_missing, ratio)
}
\arguments{
\item{G}{Integer matrix, taxa x sites, values 0/1/2/3 = REF-hom / het /
ALT-hom / missing (engine canonical \code{g}). One family, one chromosome,
sites sorted by position.}

\item{max_missing}{Max missing-genotype proportion for a site to be tested.}

\item{ratio}{Expected minor-allele frequency: 0.25/0.75 backcross, 0.5 F2.}
}
\value{
List: \code{seg} (logical, kept sites), \code{Mj}/\code{Mn} (major/minor allele
counts), \code{p_missing}, and the model probs \code{pmono}/\code{pquarter}/\code{phalf}
(NA at untested sites) — for per-stage TASSEL-parity checks.
}
\description{
Per site, keep it if its minor-allele count fits the design's segregation
model better than the alternatives. Backcross (\code{ratio} 0.25 or 0.75):
keep iff \code{pquarter > phalf && pquarter > pmono}. F2 (\code{ratio} 0.5): keep iff
\code{phalf / (pmono + pquarter) > 2}. Sites that are monomorphic (single allele)
or exceed \code{max_missing} are dropped.
}
\keyword{internal}
