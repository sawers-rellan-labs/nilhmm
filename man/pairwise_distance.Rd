% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pairwise_distance.R
\name{pairwise_distance}
\alias{pairwise_distance}
\title{Pairwise marker relatedness (r2, mutual information, or variation of information)}
\usage{
pairwise_distance(
  geno,
  method = c("r2", "mi", "vi"),
  base = c("nats", "bits"),
  max_markers = 7000L
)
}
\arguments{
\item{geno}{Numeric matrix, rows = markers, cols = samples; dosages (typically
\code{0}/\code{1}/\code{2}). Missing entries are \code{NA}/\code{NaN}. Row names (marker ids) are
carried onto both dimensions of the result.}

\item{method}{One of \code{"r2"} (default), \code{"mi"}, \code{"vi"}.}

\item{base}{For \code{mi}/\code{vi}: information unit, \code{"nats"} (default) or \code{"bits"}.
Ignored for \code{r2}.}

\item{max_markers}{Safety guard on the marker count (default \code{7000L}, the
validated scale). The output is an O(n^2) markers x markers matrix, so a
request above \code{max_markers} errors cleanly (before any large allocation)
rather than risking a \code{malloc} failure; large-but-allowed sizes warn. Raise
it (up to the ceiling in \code{getOption("nilHMM.marker_hard_cap")}, default
\code{30000}) only if the memory is available; the intended workflow prunes per
chromosome, well under the default.}
}
\value{
Symmetric numeric matrix (markers x markers) with \code{attr(, "kind")}
\code{"similarity"} (r2, mi) or \code{"distance"} (vi) and \code{attr(, "method")} the
measure. Dimnames from \code{rownames(geno)}.
}
\description{
Compute a symmetric markers x markers relatedness matrix from a
markers x samples dosage matrix, under a pluggable measure. The measure is
computed over the samples where both markers are non-missing (any \code{NA}/\code{NaN}).
The result is handed to \code{\link[=select_independent]{select_independent()}} for maximal-independent-set
marker thinning; the measure is thresholded in its own units (no r2<->MI
conversion).
}
\details{
\describe{
\item{\code{r2} (default, a similarity)}{\code{cor(g_i, g_j)^2}, the squared Pearson
correlation of the dosage rows -- the PLINK \code{--indep-pairwise} convention.
High = related. A constant (zero-variance) marker contributes \code{0}.}
\item{\code{mi} (a similarity)}{plug-in mutual information from the joint genotype
contingency table, with the Miller-Madow bias correction
\verb{I_MM = I_plugin + (m_X + m_Y - m_XY - 1) / (2N)}. High = related.}
\item{\code{vi} (a distance, a true metric)}{variation of information
\code{VI(X,Y) = H(X) + H(Y) - 2*I(X,Y)}, on MM-corrected entropies. LOW =
related; \code{VI} is a metric (obeys the triangle inequality), unlike r2/MI.}
}
For near-linearly-dependent genotypes (e.g. RIL ancestry dosages) \code{r2} and \code{mi}
rank pairs almost identically; \code{mi}/\code{vi} earn their keep on non-monotone or
multiallelic data and for the metric/embedding direction.
}
\examples{
set.seed(1)
geno <- matrix(sample(0:2, 5 * 20, replace = TRUE), nrow = 5,
               dimnames = list(paste0("m", 1:5), NULL))
d <- pairwise_distance(geno, "r2")
attr(d, "kind")   # "similarity"
}
\seealso{
\code{\link[=select_independent]{select_independent()}}
}
