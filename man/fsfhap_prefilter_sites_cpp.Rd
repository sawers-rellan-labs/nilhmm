% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fsfhap_prefilter_sites_cpp}
\alias{fsfhap_prefilter_sites_cpp}
\title{FSFHap preFilterSites (full): filterSnpsByTag -> het-deviation -> biallelic -> LD}
\usage{
fsfhap_prefilter_sites_cpp(
  G,
  pos,
  min_maf,
  min_coverage,
  max_het_deviation,
  min_r2
)
}
\arguments{
\item{G}{Integer matrix, taxa x sites, canonical \code{g} in \verb{\{0,1,2,3\}}; one chromosome.}

\item{pos}{Integer marker positions (bp), length = ncol(G).}

\item{min_maf, min_coverage, max_het_deviation, min_r2}{TASSEL fields
(0.05 / 0.2 / 5 / 0.2).}
}
\value{
Logical vector, length = ncol(G): TRUE = site kept by preFilterSites.
}
\description{
Faithful port of \code{BiparentalHaplotypeFinder.preFilterSites}. Step 1 =
\code{\link[=fsfhap_filter_snps_by_tag_cpp]{fsfhap_filter_snps_by_tag_cpp()}} with \verb{(minMaf, 1 - minCoverage, 1.0)}. On the
result: (2) drop sites whose het fraction exceeds \code{mean + maxHetDeviation * sd}
(sample sd over the filtered sites); (3) drop monomorphic (non-biallelic) sites;
(4) if \code{minR2 > 0}, drop sites whose average \code{r^2} over a ±50-filtered-site window
(hets→missing, \code{calc_rsqr}) is \verb{< minR2} (NaN pairs skipped; all-NaN average is
not a rejection, matching TASSEL's \code{NaN < minR2} = false).
}
\keyword{internal}
