% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{lb_viterbi_cpp}
\alias{lb_viterbi_cpp}
\title{LB-Impute distance-aware full-chromosome Viterbi}
\usage{
lb_viterbi_cpp(log_init, log_emit, pos, recombdist, drp)
}
\arguments{
\item{log_init}{Length-3 vector of log initial-state probabilities (REF/HET/ALT).}

\item{log_emit}{T x 3 matrix of log emissions (from \code{\link[=lb_emission_loglik_cpp]{lb_emission_loglik_cpp()}}).}

\item{pos}{Integer length-T marker positions in bp (sorted ascending).}

\item{recombdist}{Distance in bp over which recombination probability
equalizes (LB-Impute \code{recombdist}, default 1e7).}

\item{drp}{If \code{TRUE}, a homozygous->homozygous switch is priced as a single
recombination rather than a double event.}
}
\value{
Integer length-T most-likely state path (0 = REF, 1 = HET, 2 = ALT).
}
\description{
Decodes the most-likely REF/HET/ALT path under LB-Impute's distance-dependent
transition (\code{FindPath2}). Between markers a physical distance \code{d} bp apart,
the stay probability is \eqn{p_s = 0.5(1 + e^{-d/recombdist})} and the single
recombination probability is \eqn{p_r = 0.5(1 - e^{-d/recombdist})}. Homozygous
<-> heterozygous transitions cost one recombination (\eqn{p_r}); homozygous
-> the OTHER homozygous state costs two (\eqn{p_r^2}) unless \code{drp = TRUE}
(LB-Impute \code{-dr}), which prices it as a single event (for inbred / RIL
populations). Transition weights are LB-Impute's exact (un-normalized) model;
the Viterbi argmax over full-chromosome paths supersedes the original's
windowed best-path + forward/reverse consensus.
}
\keyword{internal}
