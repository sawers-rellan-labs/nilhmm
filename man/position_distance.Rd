% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/position_distance.R
\name{position_distance}
\alias{position_distance}
\title{Pairwise marker distance from map/physical coordinates}
\usage{
position_distance(pos, chr = NULL, method = "cm", max_markers = 7000L)
}
\arguments{
\item{pos}{Numeric vector of marker coordinates (cM or bp). \code{names(pos)}, if
present, are carried onto both dimensions of the result.}

\item{chr}{Optional chromosome label per marker (same length as \code{pos}; any
type comparable with \code{!=}). When given, cross-chromosome pairs are \code{Inf}.}

\item{method}{Label recorded in \code{attr(, "method")} (default \code{"cm"}); use
\code{"pos"} or \code{"bp"} for physical coordinates. Does not change the arithmetic.}

\item{max_markers}{Safety guard on the marker count (default \code{7000L}, the
validated scale). The output is an O(n^2) markers x markers matrix, so a
request above \code{max_markers} errors cleanly (before any large allocation)
rather than risking a \code{malloc} failure; large-but-allowed sizes warn. Raise
it (up to the ceiling in \code{getOption("nilHMM.marker_hard_cap")}, default
\code{30000}) only if the memory is available; the intended workflow prunes per
chromosome, well under the default.}
}
\value{
Symmetric numeric matrix (markers x markers) of \verb{|pos_i - pos_j|},
zero diagonal, with \code{attr(, "kind") = "distance"} and \code{attr(, "method")}
the label. Dimnames from \code{names(pos)} when present. Cross-chromosome pairs
are \code{Inf} when \code{chr} is supplied.
}
\description{
Build a symmetric markers x markers \strong{distance} matrix of the absolute
coordinate difference \verb{|pos_i - pos_j|} from a vector of marker positions
(genetic cM or physical bp). The result is handed to \code{\link[=select_independent]{select_independent()}}
with \code{sense = "distance"}: thresholding at \code{t} links markers \verb{<= t} apart, so
the selected independent set is a maximal set of markers pairwise \verb{> t} apart
-- distance-based thinning on the same machinery as r2/MI/VI relatedness.
}
\details{
Distance sense: an edge exists when \verb{|pos_i - pos_j| <= threshold}, so the
independent set has all pairwise gaps \verb{> threshold}. For a cM map, \code{threshold}
is in cM (e.g. \code{0.1} ~ Chen's 0.1-cM thinning). On a one-dimensional
coordinate the maximal independent set coincides with a greedy min-spacing
sweep (greedy is optimal on a line); the matrix path exists to give a uniform
interface across r2/MI/VI/cM rather than to beat the sweep.

If \code{chr} is supplied, cross-chromosome pairs are set to \code{Inf} (never within
any finite \code{threshold}, hence never linked), so a genome-wide matrix still
prunes correctly. Per-chromosome use is preferred and cheaper (the matrix is
O(n^2)); the \code{chr} argument is a convenience for a single pooled call.
}
\examples{
cm <- c(m1 = 0, m2 = 0.05, m3 = 0.2, m4 = 0.9)
d <- position_distance(cm)
attr(d, "kind")   # "distance"
# Prune to markers pairwise > 0.1 cM apart:
select_independent(d, threshold = 0.1, sense = "distance")
}
\seealso{
\code{\link[=select_independent]{select_independent()}}, \code{\link[=pairwise_distance]{pairwise_distance()}}
}
