% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{fsfhap_filter_snps_by_tag_cpp}
\alias{fsfhap_filter_snps_by_tag_cpp}
\title{FSFHap preFilterSites step 1: filterSnpsByTag (faithful port)}
\usage{
fsfhap_filter_snps_by_tag_cpp(G, pos, min_maf, max_missing, max_het)
}
\arguments{
\item{G}{Integer matrix, taxa x sites, canonical \code{g} in {0,1,2,3}; one
chromosome, sites sorted by position.}

\item{pos}{Integer marker positions (bp), length = ncol(G).}

\item{min_maf, max_missing, max_het}{Per-site gates (minor-allele freq,
missing proportion, heterozygous proportion). preFilterSites calls this with
\verb{(minMaf, 1 - minCoverage, 1.0)}.}
}
\value{
Logical vector, length = ncol(G): TRUE = kept site.
}
\description{
Thins SNPs from the same GBS tag and applies per-site quality gates. Finds the
first site passing MAF/missing/het thresholds (the head), then keeps each later
site that (is >= 64 bp from the head OR has presence-correlation \verb{< 0.7} to it)
AND passes the gates; the head advances to each kept site. Faithful to TASSEL's
\code{filterSnpsByTag(a, minMaf, maxMissing, maxHet)}.
}
\keyword{internal}
