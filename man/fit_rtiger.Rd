% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rtiger.R
\name{fit_rtiger}
\alias{fit_rtiger}
\title{Fit the RTIGER emission once (to reuse across per-chromosome decodes)}
\usage{
fit_rtiger(data, rigidity, threads = 1L, seed = 1L)
}
\arguments{
\item{data}{Long observation table (\verb{name, chr, pos, n_ref, n_alt}), as for
\code{\link[=call_states]{call_states()}}. Filter to covered markers upstream if using \code{min_reads}.}

\item{rigidity}{Integer RTIGER minimum run length.}

\item{threads, seed}{Passed to the fit (RcppParallel E-step; seeded init).}
}
\value{
An RTIGER fit \code{list(A, pi, alpha, beta, iterations)}.
}
\description{
Fits the faithful RTIGER EM (emission alpha/beta + transition) on ALL chains in
\code{data} (samples x chromosomes) and returns the fit, to be passed as
\verb{call_states(..., caller = "rtiger", rtiger_fit = <fit>)}. This keeps the joint
per-family emission while letting each chromosome be decoded in a separate,
low-memory call -- e.g. a genome-scale sweep that would otherwise hold the whole
panel per worker. Decoding is per-chromosome regardless, so results are identical
to a single whole-family call.
}
\seealso{
\code{\link[=call_states]{call_states()}}
}
