% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_family}
\alias{simulate_family}
\title{Simulate a tracked forest of BC{n}S{m} sibling families (shared ancestry)}
\usage{
simulate_family(
  design = "BC2S3",
  families = 10L,
  sibs = 10L,
  map = NULL,
  n_markers = 1000L,
  chr = NULL,
  m = 10L,
  p = 0,
  seed = NULL,
  donor = "B",
  prefix = "fam"
)
}
\arguments{
\item{design}{"BC{n}S{m}" (default "BC2S3"); needs >= 1 selfing generation.}

\item{families}{Number of independent families (independent founders).}

\item{sibs}{Genotyped siblings per family (they share the founder + chain).}

\item{map, n_markers, chr, m, p, seed, donor}{As in \code{\link[=simulate_nil]{simulate_nil()}}.}

\item{prefix}{Family-id prefix for taxon/family names (default "fam").}
}
\value{
\verb{list(truth = <per-marker table: source, donor, name, family, chr, pos, cm, state>, pedigree = <data.frame taxon, family, parent1, parent2>)}.
}
\description{
Unlike \code{\link[=simulate_nil]{simulate_nil()}} (independent lines with no relatives), this draws ONE
\pkg{simcross} meiosis per family and reads off \code{sibs} terminal siblings from
that single draw, so the sibs genuinely share the IBD blocks inherited through
the (latent, ungenotyped) founder -> selfing chain. This is the ground-truth
generator for validating a pedigree-aware refinement (\code{\link[=refine_ancestry]{refine_ancestry()}}):
the coupling refine exploits only exists when relatives share ancestry.
}
\details{
Returns both the per-marker \code{truth} for the genotyped sibs (a \code{\link[=simulate_nil]{simulate_nil()}}
table, so it feeds \code{\link[=simulate_counts]{simulate_counts()}} / \code{\link[=to_segments]{to_segments()}}) and a
\code{\link[=read_pedigree]{read_pedigree()}}-shaped \code{pedigree}: one row per taxon, with the founder and
the intermediate selfing nodes present as \strong{parent-only latent rows} (no
genotype), the sibs as leaves. Family \code{f} is a star: founder \code{g0} -> \code{g1} ->
... -> \verb{g\{m-1\}} -> \code{sibs} leaves; families are independent (independent founders).
}
\examples{
if (requireNamespace("simcross", quietly = TRUE)) {
  fam <- simulate_family("BC2S3", families = 2, sibs = 3, chr = 1,
                         n_markers = 100, seed = 1)
  head(fam$pedigree)
  table(fam$truth$name)
}
}
\seealso{
\code{\link[=simulate_nil]{simulate_nil()}}, \code{\link[=simulate_counts]{simulate_counts()}}, \code{\link[=read_pedigree]{read_pedigree()}}, \code{\link[=refine_ancestry]{refine_ancestry()}}.
}
