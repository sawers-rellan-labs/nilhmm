% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_gt.R
\name{call_gt}
\alias{call_gt}
\title{Per-site genotype caller with a swappable prior}
\usage{
call_gt(
  n_ref,
  n_alt,
  prior = "hwe",
  af = NULL,
  error = 0.01,
  return = c("call", "dosage", "post")
)
}
\arguments{
\item{n_ref, n_alt}{Reference / alternate read counts. Integer (or numeric)
matrices \verb{markers x samples}, or plain vectors (treated as one sample =
\verb{markers x 1}). Same shape required.}

\item{prior}{The single-locus prior: \code{"hwe"} (default), \code{"flat"}, or a length-3
numeric vector \code{c(f_REF, f_HET, f_ALT)} (fixed genome-wide, renormalized).}

\item{af}{For \code{prior = "hwe"}: per-marker ALT allele frequency \code{p}, length
\code{nrow}. \code{NULL} (default) self-estimates \code{p} from the reads (discouraged).}

\item{error}{Per-read error rate \code{epsilon} in \verb{(0, 0.5)} (default \code{0.01}).}

\item{return}{\code{"call"} (default): integer 0/1/2 hard call, \code{NA} at zero depth.
\code{"dosage"}: the same hard call cast to numeric (convenience for
\code{\link[=interpolate_genotype]{interpolate_genotype()}}) -- this is the hardcall as a double, \strong{not}
\code{E[G | reads]}; for the true posterior-mean dosage take \code{return = "post"} and
compute \code{gp[,,2]*1 + gp[,,3]*2}. \code{"post"}: the normalized genotype-posterior
(GP) array \verb{markers x samples x 3} (\code{NA} rows at zero depth). "GP" is the VCF
FORMAT field for \code{P(G | reads)}; the hard \code{"call"} is its MAP (argmax-GP).}
}
\value{
For \code{"call"}/\code{"dosage"}: a matrix \verb{markers x samples} (a plain vector if
the inputs were vectors). For \code{"post"}: the genotype-posterior (GP) array,
\verb{markers x samples x 3} (slices ordered REF/HET/ALT = dosage 0/1/2).
}
\description{
A closed-form GATK-style diploid genotype call from allelic read counts, decided
independently per (marker, sample) -- no linkage model. ALT = donor (teosinte) =
dosage 2. Given per-read error \code{error} and counts \verb{(n_ref, n_alt)}, the three
genotype log-likelihoods (GL) are
\deqn{L(0) = (1-\epsilon)^{n_{ref}}\,\epsilon^{n_{alt}}}
\deqn{L(2) = \epsilon^{n_{ref}}\,(1-\epsilon)^{n_{alt}}}
\deqn{L(1) = 0.5^{\,n_{ref}+n_{alt}}}
and the call is \verb{argmax_g L(g) * pi(g)} over the prior \code{pi} (the MAP / argmax-GP
estimate). All arithmetic is in log space and vectorized over the whole matrix
(no per-cell loop). Markers with zero total depth carry no signal and are
returned as \code{NA}.
}
\details{
The prior \emph{is} the argument. \code{prior} is polymorphic:
\describe{
\item{\code{"flat"}}{Uniform prior -> pure argmax-GL (the ML call). Het-blind at
depth 1 (a single ALT read is called hom-ALT), the pessimistic naive caller.}
\item{\code{"hwe"}}{Hardy-Weinberg from the per-marker ALT (donor/teosinte) allele
frequency \code{p}: \verb{pi = \{(1-p)^2, 2p(1-p), p^2\}}. This is the \strong{het-excess}
control -- a single ALT read is called HET. \code{af} supplies \code{p} per marker; if
\code{NULL}, \code{p} is self-estimated per marker from the reads
(\code{rowSums(n_alt) / rowSums(n_ref + n_alt)}) -- discouraged, as the estimate
is circular at low depth.}
\item{numeric \code{c(f_REF, f_HET, f_ALT)}}{A \strong{fixed} genome-wide prior,
renormalized to sum 1. Covers both the \strong{design} prior (a vector derived
from the cross -- see \code{\link[=breeding_prior]{breeding_prior()}}) and an arbitrary \strong{custom} prior;
the code path is identical.}
}
A length-3 numeric vector is genome-wide fixed -- it cannot express a per-marker
custom prior. (An \verb{M x 3} matrix could be accepted for that later; out of scope
now.)
}
\examples{
# A single ALT read decided under four priors:
call_gt(0, 1, prior = "flat")                 # 2 (hom-ALT, het-blind: argmax-GL)
call_gt(0, 1, prior = "hwe", af = 0.30)        # 1 (HET, the het-excess control)
call_gt(0, 1, prior = breeding_prior("BC2S3"))   # 2 (design prior resists the het flip)
call_gt(0, 1, prior = c(.98, .01, .01))        # custom fixed prior
# High depth: the data dominates the prior.
call_gt(0, 10, prior = "hwe", af = 0.05)       # 2
}
\seealso{
\code{\link[=breeding_prior]{breeding_prior()}}, \code{\link[=interpolate_genotype]{interpolate_genotype()}}, \code{\link[=call_ancestry]{call_ancestry()}}
}
