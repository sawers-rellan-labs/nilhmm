# Regenerates the bundled, version-namespaced `maize_map_v5` consensus map
# (marker -> chr, cM, bp) from its pinned source (REFACTOR_R_PACKAGE.md §6).
# Not shipped; the stored data/ value is authoritative at runtime. cM-space is
# assembly-robust; bp/Mb is tied to B73 v5 -- warn on assembly mismatch.

stop("data-raw/make_maize_map.R is a scaffold stub (Task 4 / §6).")